// Created file and the code in the file by running the following in the terminal:
//  go mod init booking-app
//      The terminal code initialize the booking-app folder and its contents as a module that can be run

module booking-app // functions as the import path for all our packages in the module

go 1.18
